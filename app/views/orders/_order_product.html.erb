<tr>
  <td class="col-sm-8 col-md-6">
    <div class="media">
      <%= link_to image_tag(order_product.product.product_images[0].picture.url,
        size: "100x100", alt: order_product.product.name, class: "media-object"),
        order_product.product.product_images[0].picture.url, class: "thumbnail pull-left" %>
      <div class="media-body">
        <h4 class="media-heading">
          <%= link_to order_product.product.name, category_product_path(
            category_id: order_product.product.category_id, id: order_product.product.id) %>
        </h4>
        <h5 class="media-heading">
            <strong>
              <%= link_to order_product.product.category.name, order_product.product.category %>
            </strong>
        </h5>
      </div>
    </div>
  </td>
  <% if order_product.order.pending? %>
    <td>
      <%= form_with url: order_detail_path(id: order_product.id), method: :patch, local: true do |f| %>
        <%= f.number_field :quantily, value: order_product.quantily,
          max: order_product.quantily + order_product.product.quantily,
          min: Settings.min_quantily, class: "form-control" %>
    </td>
    <td class="col-sm-1 col-md-1 text-center">
      <strong><%= number_to_currency order_product.price, locale: :en %></strong>
    </td>
    <td>
      <%= f.button type: :submit, class: "btn btn-info" do %>
        <% content_tag(:span, nil, class: "glyphicon glyphicon-refresh") %>
      <% end %>
      <% end %>
      <%= link_to order_detail_path(order_product.id), method: :delete, data: {confirm: "Xóa?"} do %>
        <button type="button" class="btn btn-danger">
          <span class="glyphicon glyphicon-remove"></span>
        </button>
      <% end %>
    </td>
  <% else %>
    <td class="col-sm-1 col-md-1" style="text-align: center">
         <strong><%= order_product.quantily %></strong>
    </td>
    <td class="col-sm-1 col-md-1 text-center">
      <strong><%= number_to_currency order_product.price, locale: :en %></strong>
    </td>
  <% end %>
</tr>
